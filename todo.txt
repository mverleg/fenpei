
An example of Fenpei is that state is derived, it intentionally is not a continuously running service.
This way, it can simply resume after losing contact with client nodes or after a reboot.

-q starts too few jobs (by weight instead of count?)

Use a template langauge for substitution files, rather than just Python substitutions

Make it easy to work with in interactive mode, either the interactive python interpreter or maybe Jupyter notebook

Output should probably have been a swappable class
- different readers for file formats
- different sets of output values
- compute things on-demand


